import DefaultLayout from '@/layout/defaultLayout';
import * as S from './style';

const Home = () => (
	<DefaultLayout>
		<S.ContainerStyle>
			<h2>esta e a home</h2>
		</S.ContainerStyle>
	</DefaultLayout>
);

export default Home;
