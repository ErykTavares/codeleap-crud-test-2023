import DefaultLayout from '@/layout/defaultLayout';
import { ContainerStyle } from './style';

const Home = () => (
	<DefaultLayout>
		<ContainerStyle>
			<h2>esta e a home</h2>
		</ContainerStyle>
	</DefaultLayout>
);

export default Home;
